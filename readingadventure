<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 Word Quest: The Reading Adventure!</title>
    <style>
        :root {
            --primary: #4f46e5;
            --primary-hover: #4338ca;
            --secondary: #06b6d4;
            --accent: #f59e0b;
            --success: #10b981;
            --danger: #ef4444;
            --bg-gradient: linear-gradient(135deg, #0f172a 0%, #1e1b4b 50%, #311b92 100%);
            --card-bg: rgba(255, 255, 255, 0.95);
            --card-dark: rgba(30, 41, 59, 0.9);
            --text-main: #1e293b;
            --radius-lg: 24px;
            --radius-md: 16px;
            --radius-sm: 10px;
            --shadow-glow: 0 0 25px rgba(99, 102, 241, 0.4);
            --shadow-card: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1);
        }

        * {
            box-sizing: border-box;
            user-select: none;
            -webkit-user-select: none;
            touch-action: manipulation;
            margin: 0;
            padding: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        body {
            background: var(--bg-gradient);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text-main);
            overflow-x: hidden;
            padding: 12px;
        }

        /* Responsive Container */
        #app-container {
            width: 100%;
            max-width: 960px;
            min-height: 650px;
            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: var(--radius-lg);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        /* Top Header Bar */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 24px;
            background: rgba(15, 23, 42, 0.6);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
        }

        .user-badge {
            display: flex;
            align-items: center;
            gap: 12px;
            background: rgba(255, 255, 255, 0.15);
            padding: 6px 16px;
            border-radius: 999px;
            font-weight: 700;
            font-size: 1.1rem;
        }

        .avatar-frame {
            font-size: 1.8rem;
            line-height: 1;
        }

        .stats-group {
            display: flex;
            gap: 16px;
            align-items: center;
        }

        .stat-item {
            display: flex;
            align-items: center;
            gap: 6px;
            background: rgba(0, 0, 0, 0.3);
            padding: 6px 14px;
            border-radius: 999px;
            font-weight: 800;
            font-size: 1.1rem;
            color: #fbbf24;
            border: 1px solid rgba(251, 191, 36, 0.3);
        }

        .icon-btn {
            background: rgba(255, 255, 255, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.25);
            color: white;
            width: 44px;
            height: 44px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2rem;
            transition: all 0.2s ease;
        }

        .icon-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.05);
        }

        /* Main View Container */
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 24px;
            position: relative;
            overflow-y: auto;
        }

        .screen {
            display: none;
            width: 100%;
            height: 100%;
            flex-direction: column;
            align-items: center;
            animation: fadeIn 0.3s ease-in-out forwards;
        }

        .screen.active {
            display: flex;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(8px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Buttons */
        .btn {
            padding: 14px 28px;
            border-radius: var(--radius-md);
            border: none;
            font-weight: 800;
            font-size: 1.2rem;
            cursor: pointer;
            transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            box-shadow: 0 4px 6px -1px rgba(0,0,0,0.2);
            text-decoration: none;
        }

        .btn:active {
            transform: scale(0.96);
        }

        .btn-primary {
            background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
            color: white;
            box-shadow: 0 10px 20px -5px rgba(79, 70, 229, 0.5);
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #4f46e5 0%, #4338ca 100%);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.9);
            color: #1e293b;
        }

        .btn-secondary:hover {
            background: #ffffff;
            transform: translateY(-2px);
        }

        .btn-accent {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            color: white;
            box-shadow: 0 10px 20px -5px rgba(245, 158, 11, 0.5);
        }

        .btn-audio {
            background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
            color: white;
            font-size: 2.2rem;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            box-shadow: 0 10px 25px rgba(6, 182, 212, 0.5);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(6, 182, 212, 0.7); }
            70% { box-shadow: 0 0 0 20px rgba(6, 182, 212, 0); }
            100% { box-shadow: 0 0 0 0 rgba(6, 182, 212, 0); }
        }

        /* Title Screen */
        .title-hero {
            text-align: center;
            color: white;
            margin-bottom: 24px;
        }

        .title-hero h1 {
            font-size: 3rem;
            font-weight: 900;
            text-shadow: 0 4px 10px rgba(0,0,0,0.5);
            letter-spacing: -1px;
            margin-bottom: 8px;
            background: linear-gradient(135deg, #a5b4fc, #38bdf8, #fef08a);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .character-select-grid {
            display: flex;
            gap: 20px;
            margin: 20px 0;
        }

        .char-card {
            background: rgba(255, 255, 255, 0.1);
            border: 3px solid rgba(255, 255, 255, 0.2);
            border-radius: var(--radius-md);
            padding: 16px 24px;
            cursor: pointer;
            text-align: center;
            transition: all 0.2s ease;
            color: white;
        }

        .char-card.selected {
            background: rgba(99, 102, 241, 0.3);
            border-color: #818cf8;
            transform: scale(1.08);
            box-shadow: var(--shadow-glow);
        }

        .char-avatar {
            font-size: 3.5rem;
            margin-bottom: 8px;
        }

        /* Map Screen */
        .world-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 16px;
            width: 100%;
            max-height: 480px;
            overflow-y: auto;
            padding-right: 8px;
        }

        .world-card {
            background: var(--card-bg);
            border-radius: var(--radius-md);
            padding: 18px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            box-shadow: var(--shadow-card);
            transition: all 0.2s ease;
            position: relative;
            border: 3px solid transparent;
        }

        .world-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.25);
        }

        .world-card.recommended {
            border-color: var(--accent);
            background: linear-gradient(180deg, #ffffff 0%, #fffbeb 100%);
        }

        .rec-tag {
            position: absolute;
            top: -12px;
            right: 12px;
            background: var(--accent);
            color: white;
            font-size: 0.75rem;
            font-weight: 800;
            padding: 4px 10px;
            border-radius: 999px;
            text-transform: uppercase;
            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
        }

        .world-header {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 8px;
        }

        .world-icon {
            font-size: 2.2rem;
            line-height: 1;
        }

        .world-title {
            font-weight: 800;
            font-size: 1.25rem;
            color: #0f172a;
        }

        .world-desc {
            font-size: 0.9rem;
            color: #475569;
            margin-bottom: 12px;
        }

        .world-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: auto;
        }

        .difficulty-badge {
            font-size: 0.75rem;
            font-weight: 700;
            padding: 4px 8px;
            border-radius: 6px;
            background: #e2e8f0;
            color: #334155;
        }

        .world-stars {
            color: #f59e0b;
            font-size: 1rem;
        }

        /* Gameplay Arena */
        .arena-card {
            background: var(--card-bg);
            width: 100%;
            border-radius: var(--radius-lg);
            padding: 28px;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-shadow: var(--shadow-card);
            min-height: 440px;
            position: relative;
        }

        .streak-banner {
            position: absolute;
            top: 16px;
            left: 20px;
            background: #fef3c7;
            color: #d97706;
            padding: 6px 14px;
            border-radius: 999px;
            font-weight: 800;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .audio-prompt-area {
            margin: 20px 0 30px 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
        }

        .choices-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 16px;
            width: 100%;
            max-width: 650px;
        }

        .choice-btn {
            background: #f8fafc;
            border: 3px solid #cbd5e1;
            border-radius: var(--radius-md);
            padding: 20px;
            font-size: 1.8rem;
            font-weight: 800;
            color: #1e293b;
            cursor: pointer;
            transition: all 0.2s ease;
            text-align: center;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
        }

        .choice-btn:hover {
            border-color: #818cf8;
            background: #ffffff;
            transform: translateY(-3px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        .choice-btn.correct {
            background: #dcfce7 !important;
            border-color: #22c55e !important;
            color: #15803d !important;
            animation: bounce 0.5s ease;
        }

        .choice-btn.incorrect {
            background: #fee2e2 !important;
            border-color: #ef4444 !important;
            color: #b91c1c !important;
            opacity: 0.6;
        }

        @keyframes bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.08); }
        }

        /* Scaffolding Sound Bubbles for Blending Beach */
        .sound-bubbles {
            display: flex;
            gap: 12px;
            margin-bottom: 20px;
        }

        .bubble-sound {
            background: #e0f2fe;
            border: 2px solid #0284c7;
            color: #0369a1;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.4rem;
            font-weight: 800;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .bubble-sound:hover {
            background: #bae6fd;
        }

        /* Tile Builder Mode */
        .tiles-container {
            display: flex;
            gap: 10px;
            min-height: 70px;
            padding: 10px;
            border-bottom: 3px dashed #cbd5e1;
            width: 100%;
            max-width: 500px;
            justify-content: center;
            margin-bottom: 20px;
        }

        .tile {
            background: #ffffff;
            border: 2px solid #6366f1;
            border-radius: var(--radius-sm);
            width: 54px;
            height: 54px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.6rem;
            font-weight: 800;
            color: #4338ca;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0,0,0,0.08);
        }

        .bank-tiles {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            justify-content: center;
            max-width: 500px;
        }

        /* Bottom Controls */
        .game-controls {
            margin-top: auto;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 16px;
        }

        .stuck-btn {
            background: #fef3c7;
            color: #b45309;
            border: 2px solid #fde68a;
            border-radius: 999px;
            padding: 8px 18px;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .stuck-btn:hover {
            background: #fde68a;
        }

        /* Modals & Overlays */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(15, 23, 42, 0.85);
            backdrop-filter: blur(8px);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            padding: 20px;
        }

        .modal-overlay.active {
            display: flex;
        }

        .modal-body {
            background: white;
            border-radius: var(--radius-lg);
            width: 100%;
            max-width: 550px;
            padding: 28px;
            box-shadow: var(--shadow-card);
            max-height: 90vh;
            overflow-y: auto;
        }

        .shop-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 14px;
            margin: 16px 0;
        }

        .shop-item {
            border: 2px solid #e2e8f0;
            border-radius: var(--radius-md);
            padding: 14px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }

        .shop-item.equipped {
            border-color: var(--success);
            background: #f0fdf4;
        }

        /* Celebration Screen */
        .celebrate-card {
            background: white;
            border-radius: var(--radius-lg);
            padding: 36px;
            text-align: center;
            max-width: 500px;
            width: 100%;
            box-shadow: var(--shadow-glow);
        }

        .celebrate-title {
            font-size: 2.2rem;
            font-weight: 900;
            color: #4f46e5;
            margin-bottom: 12px;
        }

        /* Helper Utilities */
        .text-center { text-align: center; }
        .hidden { display: none !important; }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            .title-hero h1 { font-size: 2rem; }
            .choices-grid { grid-template-columns: 1fr; }
            .btn-audio { width: 66px; height: 66px; font-size: 1.8rem; }
            .world-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <div id="app-container">
        <!-- Header Bar -->
        <header>
            <div class="user-badge">
                <span class="avatar-frame" id="header-avatar">🦊</span>
                <span id="header-name">Explorer</span>
            </div>
            <div class="stats-group">
                <div class="stat-item">⭐ <span id="stat-stars">0</span></div>
                <div class="stat-item">💎 <span id="stat-gems">0</span></div>
                <button class="icon-btn" onclick="game.showRewards()" title="My Rewards">🎒</button>
                <button class="icon-btn" onclick="game.showMap()" title="World Map">🗺️</button>
                <button class="icon-btn" onclick="game.toggleMute()" id="mute-btn" title="Toggle Sound">🔊</button>
                <button class="icon-btn" onclick="game.showTeacherModal()" title="Teacher Settings">⚙️</button>
            </div>
        </header>

        <!-- Main Screen Container -->
        <main>
            <!-- 1. Start Screen -->
            <div id="screen-start" class="screen active">
                <div class="title-hero">
                    <h1>🚀 WORD QUEST</h1>
                    <p style="font-size: 1.2rem; opacity: 0.9;">The Ultimate Reading Adventure!</p>
                </div>

                <div style="background: rgba(255,255,255,0.1); padding: 20px; border-radius: var(--radius-md); text-align: center; color: white; margin-bottom: 20px;">
                    <h3 style="margin-bottom: 10px;">Choose Your Character:</h3>
                    <div class="character-select-grid">
                        <div class="char-card selected" onclick="game.selectCharacter('Fox', '🦊', this)">
                            <div class="char-avatar">🦊</div>
                            <div style="font-weight: 800;">Fox</div>
                        </div>
                        <div class="char-card" onclick="game.selectCharacter('Panda', '🐼', this)">
                            <div class="char-avatar">🐼</div>
                            <div style="font-weight: 800;">Panda</div>
                        </div>
                        <div class="char-card" onclick="game.selectCharacter('Frog', '🐸', this)">
                            <div class="char-avatar">🐸</div>
                            <div style="font-weight: 800;">Frog</div>
                        </div>
                    </div>
                    <p id="welcome-msg" style="font-size: 1.1rem; color: #fef08a; font-weight: 700;">"Hi, Word Explorer! Let's unlock some words!"</p>
                </div>

                <div style="display: flex; gap: 16px; flex-wrap: wrap; justify-content: center;">
                    <button class="btn btn-primary" onclick="game.startRecommendedWorld()">▶ START ADVENTURE</button>
                    <button class="btn btn-secondary" onclick="game.showMap()">🗺️ CHOOSE WORLD</button>
                </div>
            </div>

            <!-- 2. World Map Screen -->
            <div id="screen-map" class="screen">
                <div style="text-align: center; color: white; margin-bottom: 16px;">
                    <h2 style="font-size: 2rem; font-weight: 900;">🗺️ CHOOSE YOUR ADVENTURE!</h2>
                    <p style="opacity: 0.85;">All worlds are open! Select any place to explore.</p>
                </div>
                <div class="world-grid" id="world-grid-container">
                    <!-- Dynamic injection of 9 worlds -->
                </div>
            </div>

            <!-- 3. Gameplay Arena Screen -->
            <div id="screen-gameplay" class="screen">
                <div class="arena-card">
                    <div class="streak-banner" id="streak-banner">🔥 STREAK: <span id="streak-count">0</span></div>
                    
                    <div style="margin-top: 10px; font-size: 1.1rem; font-weight: 800; color: #6366f1;" id="world-header-title">
                        🌟 Sight Word Street
                    </div>

                    <!-- Audio Prompt Button -->
                    <div class="audio-prompt-area">
                        <button class="btn btn-audio" onclick="game.playCurrentAudio()" id="main-audio-btn">🔊</button>
                        <p style="font-size: 0.95rem; color: #64748b; font-weight: 600;" id="prompt-subtext">Tap to hear the sound!</p>
                    </div>

                    <!-- Scaffolding Container (Sound bubbles / tiles if needed) -->
                    <div id="scaffold-area" class="hidden"></div>

                    <!-- Choices Grid -->
                    <div class="choices-grid" id="choices-container">
                        <!-- Dynamic Choice Buttons -->
                    </div>

                    <!-- Interactive Bottom Bar -->
                    <div class="game-controls">
                        <button class="stuck-btn" onclick="game.giveHint()">💡 I'M STUCK</button>
                        <button class="btn btn-secondary" style="padding: 8px 16px; font-size: 0.95rem;" onclick="game.showMap()">🗺️ Map</button>
                    </div>
                </div>
            </div>

            <!-- 4. Micro Story Screen -->
            <div id="screen-story" class="screen">
                <div class="arena-card" style="text-align: center;">
                    <h3 style="font-size: 1.5rem; color: #4f46e5; margin-bottom: 16px;">📖 Quick Story Time!</h3>
                    <div id="story-passage" style="background: #f8fafc; padding: 20px; border-radius: var(--radius-md); font-size: 1.5rem; font-weight: 700; color: #1e293b; margin-bottom: 24px; line-height: 1.6;">
                        <!-- Story text -->
                    </div>
                    <button class="btn btn-audio" style="margin-bottom: 20px;" onclick="game.playStoryAudio()">🔊</button>
                    <p style="font-weight: 700; font-size: 1.2rem; margin-bottom: 16px;" id="story-question"></p>
                    <div class="choices-grid" id="story-choices"></div>
                </div>
            </div>

            <!-- 5. World Clear Screen -->
            <div id="screen-celebrate" class="screen">
                <div class="celebrate-card">
                    <div style="font-size: 4rem; margin-bottom: 8px;">🎉</div>
                    <div class="celebrate-title" id="celebrate-title">WORLD COMPLETE!</div>
                    <p style="font-size: 1.1rem; color: #475569; margin-bottom: 20px;" id="celebrate-sub">You are becoming a Reading Star!</p>
                    
                    <div style="background: #f8fafc; padding: 16px; border-radius: var(--radius-md); margin-bottom: 24px; display: flex; justify-content: space-around;">
                        <div>
                            <div style="font-size: 1.5rem;">⭐</div>
                            <div style="font-weight: 800;" id="celeb-stars">+5 Stars</div>
                        </div>
                        <div>
                            <div style="font-size: 1.5rem;">💎</div>
                            <div style="font-weight: 800;" id="celeb-gems">+10 Gems</div>
                        </div>
                    </div>

                    <div style="display: flex; flex-direction: column; gap: 12px;">
                        <button class="btn btn-primary" onclick="game.replayCurrentWorld()">▶ PLAY AGAIN</button>
                        <button class="btn btn-secondary" onclick="game.showMap()">🗺️ CHOOSE ANOTHER WORLD</button>
                        <button class="btn btn-accent" onclick="game.showRewards()">🎒 VIEW REWARDS</button>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Rewards Modal -->
    <div class="modal-overlay" id="modal-rewards">
        <div class="modal-body">
            <h2 style="font-size: 1.8rem; font-weight: 900; color: #1e293b; margin-bottom: 8px;">🎒 Explorer Rewards Shop</h2>
            <p style="color: #64748b; margin-bottom: 16px;">Spend your gems to get cool accessories!</p>
            <div class="shop-grid" id="shop-grid">
                <!-- Dynamic shop items -->
            </div>
            <button class="btn btn-primary" style="width: 100%; margin-top: 16px;" onclick="game.hideRewards()">Close Shop</button>
        </div>
    </div>

    <!-- Teacher Mode Modal -->
    <div class="modal-overlay" id="modal-teacher">
        <div class="modal-body">
            <h2 style="font-size: 1.8rem; font-weight: 900; color: #1e293b; margin-bottom: 12px;">👩‍🏫 Teacher & Parent Mode</h2>
            
            <div style="background: #f1f5f9; padding: 14px; border-radius: var(--radius-sm); margin-bottom: 16px;">
                <label style="font-weight: 800; display: block; margin-bottom: 6px;">Navigation Mode:</label>
                <select id="teacher-mode-select" style="width: 100%; padding: 10px; border-radius: 8px; font-weight: 700;" onchange="game.setNavigationMode(this.value)">
                    <option value="OPEN">OPEN MODE (All worlds freely selectable)</option>
                    <option value="GUIDED">GUIDED MODE (Highlights recommended world)</option>
                </select>
            </div>

            <h4 style="font-size: 1.1rem; margin-bottom: 8px;">📊 Learner Analytics</h4>
            <div id="teacher-stats-view" style="font-size: 0.95rem; color: #334155; line-height: 1.6; background: #fafafa; padding: 12px; border-radius: 8px; margin-bottom: 16px;">
                <!-- Stats injected here -->
            </div>

            <div style="display: flex; gap: 10px;">
                <button class="btn btn-secondary" style="flex: 1;" onclick="game.resetProgress()">⚠️ Reset Progress</button>
                <button class="btn btn-primary" style="flex: 1;" onclick="game.hideTeacherModal()">Done</button>
            </div>
        </div>
    </div>

    <script>
        
        // -------------------------------------------------------------
        // SIGHT WORD DATABASE: Strictly 99 Fry words (EXCLUDING "a")
        // -------------------------------------------------------------
        const FRY_99_WORDS = [
            "the", "of", "and", "to", "in", "is", "you", "that", "it", "he",
            "was", "for", "on", "are", "as", "with", "his", "they", "I", "at",
            "be", "this", "have", "from", "or", "one", "had", "by", "word", "but",
            "not", "what", "all", "were", "we", "when", "your", "can", "said", "there",
            "use", "an", "each", "which", "she", "do", "how", "their", "if", "will",
            "up", "other", "about", "out", "many", "then", "them", "these", "so", "some",
            "her", "would", "make", "like", "him", "into", "time", "has", "look", "two",
            "more", "write", "go", "see", "number", "no", "way", "could", "people", "my",
            "than", "first", "water", "been", "call", "who", "oil", "its", "now", "find",
            "long", "down", "day", "did", "get", "come", "made", "may", "part"
        ]; // Exactly 99 words! "a" is strictly removed.

        // World Definitions
        const WORLDS_DATA = [
            {
                id: 'sight_words',
                title: 'Sight Word Street',
                icon: '🌟',
                desc: 'Hear it. Read it. Find it.',
                diff: '⭐ Beginner',
                type: 'sight'
            },
            {
                id: 'sound_cave',
                title: 'Sound Cave',
                icon: '🔊',
                desc: 'Listen for beginning, middle & ending sounds.',
                diff: '⭐ Beginner',
                type: 'phonological'
            },
            {
                id: 'blending_beach',
                title: 'Blending Beach',
                icon: '🏖️',
                desc: 'Blend the sounds together to unlock the treasure!',
                diff: '⭐⭐ Getting Ready',
                type: 'blending'
            },
            {
                id: 'word_forest',
                title: 'Word Forest',
                icon: '🌳',
                desc: 'Read simple CVC words.',
                diff: '⭐⭐ Getting Ready',
                type: 'cvc'
            },
            {
                id: 'digraph_volcano',
                title: 'Digraph Volcano',
                icon: '🌋',
                desc: 'Listen for tricky digraph sounds like sh, ch, th.',
                diff: '⭐⭐⭐ Challenge',
                type: 'digraph'
            },
            {
                id: 'blend_bridge',
                title: 'Blend Bridge',
                icon: '🌉',
                desc: 'Watch out for consonant blends!',
                diff: '⭐⭐⭐ Challenge',
                type: 'consonant_blend'
            },
            {
                id: 'word_space',
                title: 'Word Space',
                icon: '🚀',
                desc: 'Read longer 2-syllable & multi-letter words.',
                diff: '⭐⭐⭐ Challenge',
                type: 'longer_words'
            },
            {
                id: 'phrase_palace',
                title: 'Phrase Palace',
                icon: '🏰',
                desc: 'Read short 2-3 word phrases.',
                diff: '⭐⭐⭐⭐ Super Challenge',
                type: 'phrase'
            },
            {
                id: 'sentence_kingdom',
                title: 'Sentence Kingdom',
                icon: '👑',
                desc: 'Listen carefully and find the complete sentence.',
                diff: '⭐⭐⭐⭐ Super Challenge',
                type: 'sentence'
            }
        ];

        // Specific Word Lists
        const CVC_WORDS = ["cat", "cap", "can", "map", "mat", "sat", "sit", "sun", "run", "pig", "big", "dog", "hot", "bed", "hen", "cup", "fox", "leg", "top", "jam", "red", "pen", "fin", "log"];
        const DIGRAPH_WORDS = ["ship", "shop", "fish", "shell", "chip", "chat", "chin", "chop", "thin", "that", "this", "then", "when", "which"];
        const BLEND_WORDS = ["stop", "step", "spin", "slip", "clap", "flag", "frog", "crab", "green", "trip"];
        const LONGER_WORDS = ["black", "plant", "stamp", "basket", "rabbit", "sunset", "picnic", "napkin", "lunch", "jumping"];
        
        const PHRASES = [
            { audio: "the red bag", choices: ["the red bag", "the red dog", "the big bag"] },
            { audio: "a big sun", choices: ["a big sun", "a hot sun", "the big sun"] },
            { audio: "run to me", choices: ["run to me", "run to bed", "sit with me"] },
            { audio: "in the box", choices: ["in the box", "on the box", "in the fox"] }
        ];

        const SENTENCES = [
            { audio: "The cat is on the mat.", choices: ["The cat is on the mat.", "The cat is in the bag.", "The dog is on the mat."] },
            { audio: "I can see a big frog.", choices: ["I can see a big frog.", "I can see a green frog.", "He can see a big frog."] },
            { audio: "She had a red cup.", choices: ["She had a red cup.", "She has a red cap.", "She had a big cup."] }
        ];

        const MICRO_STORIES = [
            {
                text: "Sam has a cat.\nThe cat can run.\nSam is happy.",
                audio: "Sam has a cat. The cat can run. Sam is happy.",
                question: "Who has a cat?",
                choices: ["Sam", "Tom", "Ben"],
                answer: "Sam"
            },
            {
                text: "The sun is hot.\nI see a big dog.\nThe dog can jump.",
                audio: "The sun is hot. I see a big dog. The dog can jump.",
                question: "What can the dog do?",
                choices: ["jump", "run", "sleep"],
                answer: "jump"
            }
        ];

        // Web Audio Synthesizer
        class SoundFX {
            constructor() {
                this.ctx = new (window.AudioContext || window.webkitAudioContext)();
            }

            playCorrect() {
                if (game.muted) return;
                const now = this.ctx.currentTime;
                const osc = this.ctx.createOscillator();
                const gain = this.ctx.createGain();
                osc.type = 'triangle';
                osc.frequency.setValueAtTime(523.25, now); // C5
                osc.frequency.exponentialRampToValueAtTime(659.25, now + 0.1); // E5
                osc.frequency.exponentialRampToValueAtTime(783.99, now + 0.2); // G5
                gain.gain.setValueAtTime(0.3, now);
                gain.gain.exponentialRampToValueAtTime(0.01, now + 0.35);
                osc.connect(gain);
                gain.connect(this.ctx.destination);
                osc.start(now);
                osc.stop(now + 0.35);
            }

            playIncorrect() {
                if (game.muted) return;
                const now = this.ctx.currentTime;
                const osc = this.ctx.createOscillator();
                const gain = this.ctx.createGain();
                osc.type = 'sine';
                osc.frequency.setValueAtTime(220, now); // A3
                osc.frequency.setValueAtTime(196, now + 0.15); // G3
                gain.gain.setValueAtTime(0.2, now);
                gain.gain.exponentialRampToValueAtTime(0.01, now + 0.3);
                osc.connect(gain);
                gain.connect(this.ctx.destination);
                osc.start(now);
                osc.stop(now + 0.3);
            }

            playFanfare() {
                if (game.muted) return;
                const notes = [523.25, 659.25, 783.99, 1046.50];
                notes.forEach((freq, idx) => {
                    const now = this.ctx.currentTime + (idx * 0.12);
                    const osc = this.ctx.createOscillator();
                    const gain = this.ctx.createGain();
                    osc.frequency.value = freq;
                    gain.gain.setValueAtTime(0.3, now);
                    gain.gain.exponentialRampToValueAtTime(0.01, now + 0.25);
                    osc.connect(gain);
                    gain.connect(this.ctx.destination);
                    osc.start(now);
                    osc.stop(now + 0.25);
                });
            }
        }

        class WordQuestGame {
            constructor() {
                this.soundFX = new SoundFX();
                this.muted = false;
                this.character = { name: 'Fox', avatar: '🦊' };
                this.stars = 0;
                this.gems = 0;
                this.streak = 0;
                this.navMode = 'OPEN'; // 'OPEN' or 'GUIDED'
                
                this.currentWorld = null;
                this.currentQuestion = null;
                this.hintLevel = 0;
                this.attemptsCount = 0;
                this.totalCorrect = 0;
                this.totalAttempts = 0;

                // Persistence data structure
                this.worldProgress = {};
                WORLDS_DATA.forEach(w => {
                    this.worldProgress[w.id] = { stars: 0, completed: false, attempts: 0, accuracy: 0 };
                });

                this.equippedHat = null;
                this.shopItems = [
                    { id: 'hat_crown', name: 'Golden Crown', icon: '👑', cost: 15 },
                    { id: 'hat_cap', name: 'Cool Cap', icon: '🧢', cost: 10 },
                    { id: 'hat_glasses', name: 'Star Specs', icon: '🕶️', cost: 20 },
                    { id: 'pet_dragon', name: 'Mini Dragon', icon: '🐲', cost: 30 }
                ];

                this.loadSaveData();
            }

            init() {
                this.updateHeaderUI();
                this.renderWorldMap();
                this.renderShop();
            }

            // Audio Synthesis (TTS)
            speak(text, rate = 0.85) {
                if (this.muted) return;
                window.speechSynthesis.cancel();
                const utterance = new SpeechSynthesisUtterance(text);
                utterance.rate = rate;
                utterance.pitch = 1.0;
                utterance.lang = 'en-US';
                window.speechSynthesis.speak(utterance);
            }

            toggleMute() {
                this.muted = !this.muted;
                document.getElementById('mute-btn').innerText = this.muted ? '🔇' : '🔊';
            }

            // Character Select
            selectCharacter(name, avatar, el) {
                document.querySelectorAll('.char-card').forEach(c => c.classList.remove('selected'));
                el.classList.add('selected');
                this.character = { name, avatar };
                document.getElementById('header-avatar').innerText = avatar;
                document.getElementById('header-name').innerText = name;
                this.speak(`Hi ${name}! Let's start!`);
                this.saveData();
            }

            // Screen Navigation
            showScreen(id) {
                document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
                document.getElementById(id).classList.add('active');
            }

            showMap() {
                this.renderWorldMap();
                this.showScreen('screen-map');
            }

            // Map Rendering & Recommendation Logic
            renderWorldMap() {
                const container = document.getElementById('world-grid-container');
                container.innerHTML = '';

                // Recommendation calculation (find first uncompleted or lowest stars world)
                let recWorldId = WORLDS_DATA[0].id;
                for (let w of WORLDS_DATA) {
                    if (!this.worldProgress[w.id].completed) {
                        recWorldId = w.id;
                        break;
                    }
                }

                WORLDS_DATA.forEach(w => {
                    const prog = this.worldProgress[w.id];
                    const isRec = (w.id === recWorldId);
                    
                    const card = document.createElement('div');
                    card.className = `world-card ${isRec ? 'recommended' : ''}`;
                    card.onclick = () => this.startWorld(w);

                    card.innerHTML = `
                        ${isRec ? `<div class="rec-tag">⭐ RECOMMENDED FOR YOU</div>` : ''}
                        <div class="world-header">
                            <div class="world-icon">${w.icon}</div>
                            <div>
                                <div class="world-title">${w.title}</div>
                                <div class="difficulty-badge">${w.diff}</div>
                            </div>
                        </div>
                        <div class="world-desc">${w.desc}</div>
                        <div class="world-meta">
                            <div class="world-stars">${'⭐'.repeat(prog.stars) || '☆☆☆☆☆'}</div>
                            <button class="btn btn-primary" style="padding: 6px 12px; font-size: 0.9rem;">EXPLORE</button>
                        </div>
                    `;
                    container.appendChild(card);
                });
            }

            startRecommendedWorld() {
                let recWorld = WORLDS_DATA[0];
                for (let w of WORLDS_DATA) {
                    if (!this.worldProgress[w.id].completed) {
                        recWorld = w;
                        break;
                    }
                }
                this.startWorld(recWorld);
            }

            // Gameplay Loop Generator
            startWorld(world) {
                this.currentWorld = world;
                this.attemptsCount = 0;
                document.getElementById('world-header-title').innerText = `${world.icon} ${world.title}`;
                this.nextQuestion();
                this.showScreen('screen-gameplay');
            }

            replayCurrentWorld() {
                if (this.currentWorld) {
                    this.startWorld(this.currentWorld);
                } else {
                    this.showMap();
                }
            }

            nextQuestion() {
                this.hintLevel = 0;
                document.getElementById('scaffold-area').classList.add('hidden');
                document.getElementById('scaffold-area').innerHTML = '';
                
                if (this.attemptsCount >= 5) {
                    // World session complete after 5 challenges
                    this.completeWorldSession();
                    return;
                }

                this.attemptsCount++;
                const wType = this.currentWorld.type;
                let qData = {};

                if (wType === 'sight') {
                    // Pick 1 target sight word from 99 Fry words
                    const target = FRY_99_WORDS[Math.floor(Math.random() * FRY_99_WORDS.length)];
                    // Generate 2 visually similar distractors
                    let distractors = FRY_99_WORDS.filter(w => w !== target && (w[0] === target[0] || Math.abs(w.length - target.length) <= 1));
                    if (distractors.length < 2) distractors = ["the", "in", "it"];
                    distractors = distractors.sort(() => 0.5 - Math.random()).slice(0, 2);
                    
                    qData = {
                        targetText: target,
                        speakAudio: target,
                        choices: [target, ...distractors].sort(() => 0.5 - Math.random())
                    };
                } else if (wType === 'phonological') {
                    const targetWord = CVC_WORDS[Math.floor(Math.random() * CVC_WORDS.length)];
                    const firstSound = targetWord[0];
                    const otherSounds = ["s", "m", "t", "b", "p", "f"].filter(s => s !== firstSound).slice(0, 2);
                    
                    qData = {
                        targetText: firstSound,
                        speakAudio: targetWord,
                        promptText: `What sound starts "${targetWord}"?`,
                        choices: [`/${firstSound}/`, `/${otherSounds[0]}/`, `/${otherSounds[1]}/`].sort(() => 0.5 - Math.random()),
                        isPhonemeMatch: true,
                        correctVal: `/${firstSound}/`
                    };
                } else if (wType === 'blending') {
                    const word = CVC_WORDS[Math.floor(Math.random() * CVC_WORDS.length)];
                    const phonemes = word.split('').join(' ... ');
                    let dists = CVC_WORDS.filter(w => w !== word && w[0] === word[0]).slice(0, 2);
                    if (dists.length < 2) dists = ["map", "cat", "sun"];
                    
                    qData = {
                        targetText: word,
                        speakAudio: phonemes,
                        choices: [word, ...dists].sort(() => 0.5 - Math.random()),
                        isBlending: true,
                        phonemesArray: word.split('')
                    };
                } else if (wType === 'cvc') {
                    const target = CVC_WORDS[Math.floor(Math.random() * CVC_WORDS.length)];
                    let dists = CVC_WORDS.filter(w => w !== target && (w[0] === target[0] || w[2] === target[2])).slice(0, 2);
                    if (dists.length < 2) dists = ["cat", "cap", "can"];
                    qData = { targetText: target, speakAudio: target, choices: [target, ...dists].sort(() => 0.5 - Math.random()) };
                } else if (wType === 'digraph') {
                    const target = DIGRAPH_WORDS[Math.floor(Math.random() * DIGRAPH_WORDS.length)];
                    let dists = DIGRAPH_WORDS.filter(w => w !== target).slice(0, 2);
                    qData = { targetText: target, speakAudio: target, choices: [target, ...dists].sort(() => 0.5 - Math.random()) };
                } else if (wType === 'consonant_blend') {
                    const target = BLEND_WORDS[Math.floor(Math.random() * BLEND_WORDS.length)];
                    let dists = BLEND_WORDS.filter(w => w !== target).slice(0, 2);
                    qData = { targetText: target, speakAudio: target, choices: [target, ...dists].sort(() => 0.5 - Math.random()) };
                } else if (wType === 'longer_words') {
                    const target = LONGER_WORDS[Math.floor(Math.random() * LONGER_WORDS.length)];
                    let dists = LONGER_WORDS.filter(w => w !== target).slice(0, 2);
                    qData = { targetText: target, speakAudio: target, choices: [target, ...dists].sort(() => 0.5 - Math.random()) };
                } else if (wType === 'phrase') {
                    const item = PHRASES[Math.floor(Math.random() * PHRASES.length)];
                    qData = { targetText: item.audio, speakAudio: item.audio, choices: item.choices.sort(() => 0.5 - Math.random()) };
                } else if (wType === 'sentence') {
                    const item = SENTENCES[Math.floor(Math.random() * SENTENCES.length)];
                    qData = { targetText: item.audio, speakAudio: item.audio, choices: item.choices.sort(() => 0.5 - Math.random()) };
                }

                this.currentQuestion = qData;
                this.renderGameplayUI();
                this.playCurrentAudio();
            }

            renderGameplayUI() {
                const container = document.getElementById('choices-container');
                container.innerHTML = '';
                
                document.getElementById('prompt-subtext').innerText = this.currentQuestion.promptText || 'Tap 🔊 to hear the audio prompt!';

                this.currentQuestion.choices.forEach(choice => {
                    const btn = document.createElement('button');
                    btn.className = 'choice-btn';
                    btn.innerText = choice;
                    btn.onclick = () => this.handleChoice(choice, btn);
                    container.appendChild(btn);
                });
            }

            playCurrentAudio() {
                if (this.currentQuestion) {
                    this.speak(this.currentQuestion.speakAudio);
                }
            }

            handleChoice(selected, btnEl) {
                this.totalAttempts++;
                const isCorrect = this.currentQuestion.isPhonemeMatch 
                    ? (selected === this.currentQuestion.correctVal)
                    : (selected === this.currentQuestion.targetText);

                if (isCorrect) {
                    btnEl.classList.add('correct');
                    this.soundFX.playCorrect();
                    this.streak++;
                    this.stars += 1;
                    this.gems += 2;
                    this.totalCorrect++;
                    this.updateHeaderUI();

                    setTimeout(() => {
                        this.nextQuestion();
                    }, 1200);
                } else {
                    btnEl.classList.add('incorrect');
                    this.soundFX.playIncorrect();
                    this.streak = 0;
                    this.updateHeaderUI();
                    this.speak("Not quite! Let's listen again.");
                }
            }

            giveHint() {
                this.hintLevel++;
                const q = this.currentQuestion;
                if (!q) return;

                if (this.hintLevel === 1) {
                    this.speak("Listen carefully again.");
                    this.playCurrentAudio();
                } else if (this.hintLevel === 2) {
                    this.speak(`It starts with the sound ${q.targetText[0]}`);
                } else if (this.hintLevel >= 3 && q.isBlending) {
                    // Show interactive sound bubbles scaffold
                    const scaffold = document.getElementById('scaffold-area');
                    scaffold.classList.remove('hidden');
                    scaffold.innerHTML = '';
                    
                    const bubbleWrap = document.createElement('div');
                    bubbleWrap.className = 'sound-bubbles';
                    q.phonemesArray.forEach(p => {
                        const b = document.createElement('div');
                        b.className = 'bubble-sound';
                        b.innerText = p;
                        b.onclick = () => this.speak(p);
                        bubbleWrap.appendChild(b);
                    });
                    scaffold.appendChild(bubbleWrap);
                    this.speak("Tap each sound bubble to hear its sound!");
                } else {
                    this.speak("Try your best! You can do it!");
                }
            }

            completeWorldSession() {
                this.soundFX.playFanfare();
                const prog = this.worldProgress[this.currentWorld.id];
                prog.stars = Math.min(5, prog.stars + 1);
                prog.completed = true;
                
                document.getElementById('celebrate-title').innerText = `${this.currentWorld.title} Cleared!`;
                document.getElementById('celeb-stars').innerText = `+5 Stars`;
                document.getElementById('celeb-gems').innerText = `+10 Gems`;
                this.stars += 5;
                this.gems += 10;
                this.updateHeaderUI();
                this.saveData();

                // Check for Micro Story Trigger
                if (Math.random() > 0.5) {
                    this.showMicroStory();
                } else {
                    this.showScreen('screen-celebrate');
                }
            }

            showMicroStory() {
                const story = MICRO_STORIES[Math.floor(Math.random() * MICRO_STORIES.length)];
                this.currentStory = story;

                document.getElementById('story-passage').innerText = story.text;
                document.getElementById('story-question').innerText = story.question;

                const choicesBox = document.getElementById('story-choices');
                choicesBox.innerHTML = '';

                story.choices.forEach(ch => {
                    const btn = document.createElement('button');
                    btn.className = 'choice-btn';
                    btn.innerText = ch;
                    btn.onclick = () => {
                        if (ch === story.answer) {
                            btn.classList.add('correct');
                            this.soundFX.playCorrect();
                            setTimeout(() => this.showScreen('screen-celebrate'), 1000);
                        } else {
                            btn.classList.add('incorrect');
                            this.soundFX.playIncorrect();
                        }
                    };
                    choicesBox.appendChild(btn);
                });

                this.showScreen('screen-story');
                this.playStoryAudio();
            }

            playStoryAudio() {
                if (this.currentStory) {
                    this.speak(this.currentStory.audio);
                }
            }

            updateHeaderUI() {
                document.getElementById('stat-stars').innerText = this.stars;
                document.getElementById('stat-gems').innerText = this.gems;
                document.getElementById('streak-count').innerText = this.streak;
            }

            // Rewards & Shop
            showRewards() {
                document.getElementById('modal-rewards').classList.add('active');
            }

            hideRewards() {
                document.getElementById('modal-rewards').classList.remove('active');
            }

            renderShop() {
                const grid = document.getElementById('shop-grid');
                grid.innerHTML = '';
                this.shopItems.forEach(item => {
                    const isEquipped = this.equippedHat === item.id;
                    const el = document.createElement('div');
                    el.className = `shop-item ${isEquipped ? 'equipped' : ''}`;
                    el.innerHTML = `
                        <div style="font-size: 2.5rem;">${item.icon}</div>
                        <div style="font-weight: 800; font-size: 0.95rem;">${item.name}</div>
                        <button class="btn btn-secondary" style="padding: 6px 12px; font-size: 0.85rem;" onclick="game.buyItem('${item.id}', ${item.cost})">
                            ${isEquipped ? 'Equipped' : `💎 ${item.cost}`}
                        </button>
                    `;
                    grid.appendChild(el);
                });
            }

            buyItem(id, cost) {
                if (this.equippedHat === id) {
                    this.equippedHat = null;
                } else if (this.gems >= cost) {
                    this.gems -= cost;
                    this.equippedHat = id;
                    this.soundFX.playFanfare();
                } else {
                    this.speak("Earn more gems by reading words!");
                }
                this.updateHeaderUI();
                this.renderShop();
                this.saveData();
            }

            // Teacher Dashboard & Modes
            showTeacherModal() {
                const statsBox = document.getElementById('teacher-stats-view');
                const acc = this.totalAttempts > 0 ? Math.round((this.totalCorrect / this.totalAttempts) * 100) : 100;
                statsBox.innerHTML = `
                    <p><strong>Total Words Attempted:</strong> ${this.totalAttempts}</p>
                    <p><strong>Accuracy Rate:</strong> ${acc}%</p>
                    <p><strong>Stars Earned:</strong> ${this.stars}</p>
                    <p><strong>Gems Earned:</strong> ${this.gems}</p>
                    <p><strong>Current Mode:</strong> ${this.navMode}</p>
                `;
                document.getElementById('modal-teacher').classList.add('active');
            }

            hideTeacherModal() {
                document.getElementById('modal-teacher').classList.remove('active');
            }

            setNavigationMode(mode) {
                this.navMode = mode;
                this.saveData();
            }

            resetProgress() {
                if (confirm("Reset all progress, stars and gems?")) {
                    localStorage.removeItem('wordQuest_saveData');
                    location.reload();
                }
            }

            saveData() {
                const data = {
                    stars: this.stars,
                    gems: this.gems,
                    character: this.character,
                    worldProgress: this.worldProgress,
                    equippedHat: this.equippedHat,
                    navMode: this.navMode,
                    totalCorrect: this.totalCorrect,
                    totalAttempts: this.totalAttempts
                };
                localStorage.setItem('wordQuest_saveData', JSON.stringify(data));
            }

            loadSaveData() {
                const raw = localStorage.getItem('wordQuest_saveData');
                if (raw) {
                    try {
                        const parsed = JSON.parse(raw);
                        this.stars = parsed.stars || 0;
                        this.gems = parsed.gems || 0;
                        this.character = parsed.character || { name: 'Fox', avatar: '🦊' };
                        this.worldProgress = parsed.worldProgress || this.worldProgress;
                        this.equippedHat = parsed.equippedHat || null;
                        this.navMode = parsed.navMode || 'OPEN';
                        this.totalCorrect = parsed.totalCorrect || 0;
                        this.totalAttempts = parsed.totalAttempts || 0;
                    } catch (e) {
                        console.error(e);
                    }
                }
            }
        }

        // Global Game Instance
        let game;
        window.onload = function() {
            game = new WordQuestGame();
            game.init();
        };
    </script>
</body>
</html>
